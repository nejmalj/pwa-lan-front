import type { Meta, StoryObj } from "@storybook/react";

import IconText from "./InfoTournament";

const meta: Meta<typeof IconText> = {
  component: IconText,
  tags: ["autodocs"]
};
export default meta;

type Story = StoryObj<typeof IconText>;

export const Default: Story = {};
